# Bron

Game engine. Built with C++ and OpenGL.
I have been working on this for about 2 years, but it is never really done.

The repository holds two things:

- **Bron Engine** (`Engine/`) - the static library: renderer, scene, physics, platform layer.
- **Bron Editor** (`Editor/`) - the application built on top of it.
